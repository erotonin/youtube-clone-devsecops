apiVersion: v1
kind: Service
metadata:
  name: youtube-clone-service
  namespace: application
spec:
  type: LoadBalancer
  selector:
    app: youtube-clone
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
